using otraClase;
using otraClase1;

class prueba
{
      int x,y;
  double resul(double z, int a,int p){
    z=z+1;
    write(z);
    return 1;
  }
  
    int result(double z, int a,int p){
      z=z+1;
      write(z);
      return 1;
    }
    
    class miclase {
        string pos;
        int neg;
        int xd;
    }
    
    double? pruebat(double z){
      z=z+1;
      write(z);
      inst jk;
      jk=new miclase;
      return jk.neg;
    }
            
  
    void Main ()
    {
      int p;
      double z;
      double? dxd;
      inst kl;
      inst xd;
      kl=new miclase;
      xd= new miclase;
      
      kl.neg++;
      xd.neg++;
      write(kl.neg);
      write(xd.neg);
    }
}