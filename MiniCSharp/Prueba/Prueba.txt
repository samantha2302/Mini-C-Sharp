using otraClase;
using otraClase1;

class prueba
{
      int x,y;
  int resul(double b, int a,int p){
    write(b);
    return 1;
  }
    void Main ()
    {
      int p;
      double z;
      x=10;
      y=10;
      p++;
      x++;
      //x=x+y+100;
      p--;
      p--;
      x--;
      //write(x);
      //write(p);
      z=1;
      write(z);
      resul(z,1,1);
      write(resul(z,1,1)+1);
    }
}